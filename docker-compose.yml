version: '3.8'

services:
  openvid:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: openvid-app
    restart: unless-stopped
    ports:
      - "3050:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOSTNAME=0.0.0.0
